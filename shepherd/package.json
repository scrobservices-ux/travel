{
  "name": "shepherd",
  "version": "1.0.0",
  "private": true,
  "description": "Congregation coordination for a body of elders — publisher, elders and administration workspaces.",
  "scripts": {
    "start": "node server/server.js",
    "test": "node test/server.js && node test/roles.js && node test/notify.js",
    "test:browser": "node test/smoke.js && node test/interact.js && node test/shared.js && node test/paperwork.js && node test/scheduling.js && node test/mobile.js"
  },
  "engines": { "node": ">=18" }
}
